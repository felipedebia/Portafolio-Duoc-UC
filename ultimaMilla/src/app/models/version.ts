export interface Version{
      moveCambios: string;
      moveFhLanzamiento: string;
      moveId: number;
      moveObligatoria: boolean;
      moveProduccion: boolean;
      moveVersion: string;
      moveVersionCode:number;
      moveVersionLinkIOS:string;
      moveVersionLinkAndroid:string;

}