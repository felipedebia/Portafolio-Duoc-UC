<%- include('partials/nav.ejs') %>
        <!-- your content here -->
          <div class="content">
            <div class="container-fluid">
              <div class="row">
                <div class="col-md-8">
                  <div class="card">
                    <div class="card-header card-header-primary">
                      <h4 class="card-title">Viendo el perfil de <%= data[0].nombre%> <%= data[0].apellido%></h4>
                    </div>
                    <div class="card-body">
                      <div class="card-text">
                        <div class="row mt-4">
                          <div class="col-xs-6 col-sm-6 col-md-6">
                            <div class="form-group">
                              <label>Nombre</label>
                              <input class="form-control mb-4" readonly id="nombre" type="text" name="nombre" placeholder="Nombre" maxLength="25" required value="<%= data[0].nombre%>">
                            </div>
                          </div>
                          <div class="col-xs-6 col-sm-6 col-md-6">
                            <div class="form-group">
                              <label>Apellido</label>
                              <input class="form-control mb-4" readonly id="apellido" type="text" name="apellido" placeholder="Apellido" maxLength="25" required value="<%= data[0].apellido%>">
                            </div>
                          </div>
                        </div>
                        <label>Correo electrónico</label>
                        <input class="form-control mb-4" readonly id="inputEmail" type="email" name="correo" placeholder="Correo electrónico" maxLength="30" required value="<%= data[0].correo%>">
                        <label>Número de documento</label>
                        <input class="form-control mb-4" readonly id="inputNumDocumento" type="text" name="num_documento" placeholder="Número de documento" maxLength="20" required value="<%= data[0].num_documento%>">
                        <label>Fecha de nacimiento</label>
                        <input class="form-control mb-4" readonly id="inputFechaNacimiento" type="date" name="fecha_nacimiento" placeholder="Fecha de nacimiento" required value="<%= data[0].fecha_nacimiento%>">
                        <label>Teléfono</label>
                        <input class="form-control mb-4" readonly id="inputTelefono" type="number" name="telefono" placeholder="Teléfono" maxLength="15" required value="<%= data[0].telefono%>">
                        <div class="row mb-4">
                          <div class="col-xs-4 col-sm-4 col-md-4">
                            <div class="form-group">
                              <label>Género</label>
                                <% if(data[0].genero == 1){ %>
                                  <input class="form-control mb-4" readonly id="inputGenero" type="text" name="genero" maxLength="30" required value="Femenino">
                                <% } else { %>
                                  <input class="form-control mb-4" readonly id="inputGenero" type="text" name="genero" maxLength="30" required value="Masculino">
                                <% } %>
                            </div>
                          </div>
                          <div class="col-xs-4 col-sm-4 col-md-4">
                            <div class="form-group">
                              <label>Tipo de usuario</label>
                              <% if(data[0].fk_id_tipo == 1){ %>
                                <input class="form-control mb-4" readonly id="inputTipo" type="text" name="tipo_usuario" maxLength="50" required value="Administrador">
                              <% } %>
                              <% if(data[0].fk_id_tipo == 2){ %>
                                <input class="form-control mb-4" readonly id="inputTipo" type="text" name="tipo_usuario" maxLength="50" required value="Transportista">
                              <% } %>
                              <% if(data[0].fk_id_tipo == 3){ %>
                                <input class="form-control mb-4" readonly id="inputTipo" type="text" name="tipo_usuario" maxLength="50" required value="Cliente Externo">
                              <% } %>
                              <% if(data[0].fk_id_tipo == 4){ %>
                                <input class="form-control mb-4" readonly id="inputTipo" type="text" name="tipo_usuario" maxLength="50" required value="Cliente Interno">
                              <% } %>
                              <% if(data[0].fk_id_tipo == 5){ %>
                                <input class="form-control mb-4" readonly id="inputTipo" type="text" name="tipo_usuario" maxLength="50" required value="Productor">
                              <% } %>
                              <% if(data[0].fk_id_tipo == 6){ %>
                                <input class="form-control mb-4" readonly id="inputTipo" type="text" name="tipo_usuario" maxLength="50" required value="Consultor">
                              <% } %>
                            </div>
                          </div>
                          <div class="col-xs-4 col-sm-4 col-md-4">
                            <div class="form-group">
                              <label>Estado de cuenta</label>
                              <% if(data[0].fk_id_estado == 1){ %>
                                <input class="form-control mb-4" readonly id="inputEstado" type="text" name="estado_cuenta" maxLength="50" required value="Activada">
                              <% } %>
                              <% if(data[0].fk_id_estado == 2){ %>
                                <input class="form-control mb-4" readonly id="inputEstado" type="text" name="estado_cuenta" maxLength="50" required value="Desactivada">
                              <% } %>
                            </div>
                          </div>
                        </div>
                        <a href="/modificarUsuario/<%= data[0].id_usuario %>"><button class="btn btn-primary mb-2 pull-right" type="button">Modificar perfil</button></a>
                        </div>
                    </div>
                  </div>
                </div>
                <div class="col-md-4">
                  <div class="card card-profile">
                    <div class="card-avatar">
                      <img class="img" src="../images/profile_pic.jpg" />
                    </div>
                    <div class="card-body">
                      <h6 class="card-category text-gray"><%= data[0].tipo_usuario_texto%></h6>
                      <h4 class="card-title"><%= data[0].nombre%> <%= data[0].apellido%></h4>
                      <p class="card-description">
                        Don't be scared of the truth because we need to restart the human foundation in truth And I love you like Kanye loves Kanye I love Rick Owens’ bed design but the back is...
                      </p>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
<%- include('partials/footer.ejs') %>

</body>
</html>